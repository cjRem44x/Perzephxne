import(t = "../src/logic")

test "swap" {
    a: i32 = 1
    b: i32 = 2
    t.swap(&a, &b)
    @assert(a == 2)
    @assert(b == 1)
}

test "translate" {
    p: t.Point = t.Point{.x=1, .y=1}
    t.translate(&p, 4, 5)
    @assert(p.x == 5)
    @assert(p.y == 6)
}
