struct vec2 { x: i32, y: i32 }

impl vec2 {
    fn new(x: i32, y: i32) -> vec2 { ret vec2{.x=x, .y=y} }
    fn print(self: ^vec2) {
        @pf("x = {self.x}, y = {self.y}\n")
    }
}

fn main() -> i32 {
    v2: *vec2 = @alo(vec2)
    v2.* = vec2.new(401, 456)
    v2.print()
    ret 0
}
