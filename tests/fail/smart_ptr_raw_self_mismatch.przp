struct vec2 { x: i32, y: i32 }

impl vec2 {
    fn new(x: i32, y: i32) -> vec2 { ret vec2{.x=x, .y=y} }
    fn print(slf: *vec2) {
        @pf("x = {slf.x}, y = {slf.y}\n")
    }
}

fn main() -> i32 {
    v2: ^vec2 = @new(vec2)
    v2.^ = vec2.new(401, 456)
    v2.print()
    ret 0
}
