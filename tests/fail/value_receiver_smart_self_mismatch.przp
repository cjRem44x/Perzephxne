struct vec2 { x: i32, y: i32 }

impl vec2 {
    fn new(x: i32, y: i32) -> vec2 { ret vec2{.x=x, .y=y} }
    fn print(self: ^vec2) { @pf("x = {self.x}, y = {self.y}\n") }
}

fn main() -> i32 {
    v := vec2.new(4, 12)
    v.print()
    ret 0
}
