struct vec2 { x: i32, y: i32 }

impl vec2 {
    fn new(x: i32, y: i32) -> vec2 { ret vec2{.x=x, .y=y} }
    fn mutate_byval(self: vec2) {
        self.x = 999
        self.y = 999
    }
}

fn main() -> i32 {
    v := vec2.new(1, 2)
    v.mutate_byval()
    @pf("{v.x} {v.y}\n")
    ret 0
}
