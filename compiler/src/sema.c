#include "sema.h"

/* Minimal semantic pass — enough to let codegen run.
   Full type inference and checking will be expanded iteratively. */

int sema_check(Module *mod) {
    /* placeholder — return success */
    (void)mod;
    return 1;
}
