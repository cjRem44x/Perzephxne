# compiled project outputs (no extension = ELF binary)
**/main
*.o
*.out

# przp run's incremental-build dependency sidecar (<binary>.d)
*.d

# przp test's compiled test binary and its discovered-test manifest
*_test
*.tests

# build artifacts
compiler/przp.dSYM/

# test run artifacts
test_output.txt
tests/tmp/
